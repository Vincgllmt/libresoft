<%- include('../partials/header.ejs') %>
    <div>
        <div class="w-100 bg-primary p-3">
            <h4 class="text-white"><a href="/" class="text-white text-decoration-none">🠔</a><span
                    class="m-3">Utilisateurs de <%= software.name %></span></h4>
        </div>
        <div class="d-flex justify-content-center m-4 flex-wrap">
            <% software.users.forEach(user => { %>
                <a href="<%= user.url %>" class="text-decoration-none">
                    <div class="card" style="width: 18rem;">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= user.name %>
                            </h5>
                            <span class="badge bg-danger">
                                <%= user.type %>
                            </span>
                        </div>
                    </div>
                </a>
                <% }) %>
        </div>
    </div>
    <%- include('../partials/footer.ejs') %>